"""
Run a simple RL algorithm with the module.
"""
import swarmrl as srl

# model = srl.Model()  # Define the RL model.
# engine = srl.Espresso()  # define the engine.
# environment = srl.Environment(model)  # Define the environment.
# environment.train_model()  # train the RL model.
# environment.run_simulation()  # Run a production simulation with a static model.
